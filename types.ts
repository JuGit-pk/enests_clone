export interface IHomePageQuery {
  // pageTitle: string;
  heroImage: { url: string; alternativeText: string };
  mainHeading: string;
  subHeading: string;
}
export interface IReview {
  reviewSite: string;
  reviewMsg: string;
  date: string;
}
